<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>You're Following</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 text-gray-900 p-10">
  <h1 class="text-2xl font-bold mb-6">👉 You're Following</h1>
  <a href="/profile" class="text-blue-600 mb-4 block">← Back to Profile</a>

  <% if (following.length === 0) { %>
    <p class="text-gray-600">You're not following anyone yet.</p>
  <% } else { %>
    <ul class="space-y-4">
      <% following.forEach(f => { %>
        <li class="p-4 w-1/2 bg-white rounded shadow flex items-center gap-4">
          <img src="/images/uploads/<%= f.profilepic %>" class="w-10 h-10 rounded-full object-cover border" />
          <div>
            <p class="text-sm font-medium">@<%= f.username %></p>
            <p class="text-xs text-gray-500"><%= f.email %></p>
          </div>
          <form action="/unfollow/<%= f._id %>" method="POST">
              <button class="px-3 py-1 bg-red-500 text-white rounded hover:bg-red-600 text-sm">Unfollow</button>
           </form>
        </li>
      <% }) %>
    </ul>
  <% } %>
</body>
</html>
