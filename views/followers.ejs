<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Your Followers</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 text-gray-900 p-10">
  <h1 class="text-2xl font-bold mb-6">👥 Your Followers</h1>
  <a href="/profile" class="text-blue-600 mb-4 block">← Back to Profile</a>

  <% if (followers.length === 0) { %>
    <p class="text-gray-600">You have no followers yet.</p>
  <% } else { %>
    <ul class="space-y-4">
      <% followers.forEach(f => { %>
        <li class="w-1/2 p-4 bg-white rounded shadow flex items-center gap-4">
          <a href="/user/<%= f._id %>" class="flex items-center gap-4 w-full">
          <img src="/images/uploads/<%= f.profilepic %>" class="w-10 h-10 rounded-full object-cover border" />
          <div>
            <p class="text-sm font-medium">@<%= f.username %></p>
            <p class="text-xs text-gray-500"><%= f.email %></p>
          </div>
          </a>
        </li>
      <% }) %>
    </ul>
  <% } %>
</body>
</html>
